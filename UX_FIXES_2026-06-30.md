# UX fixes from the week-of-2026-06-30 session map-reduce

Source: map-reduce over the past week of finance-app sessions (162 raw findings ->
41 themes), each git-checked against this repo + the two gap docs, plus a live
review of the Todoist Finance board. Branch `feat/ux-fixes-2026-06-30`.

## Shipped on this branch (7 commits, 568 tests green)

| Commit | Theme | What changed |
|---|---|---|
| a77c62a | job-health-requires-date, decision-action-param | `get_job_health` defaults `as_of_date` to today (no-arg daily call no longer fails); `record_charge_onboarding_decision` accepts a bare action string and the `decision` key alias |
| 009d62d | false-floor-breach-future-date | `cashflow.py` rolls the balance snapshot forward to `start_date` when the snapshot predates it, so a future-dated projection includes upcoming income instead of reporting a phantom cash-floor breach |
| 34e3974 | surfaced-task-content-quality (T1) | onboarding digest pluralizes "charge"/"charges" (was "1 charges to review") |
| 5a6e0b5 | no-transaction-list-tool | new read-only `list_transactions` tool (date range, text search, amount floor, account filter) so the agent can quote exact charge amounts |
| 5e664d1 | balance-resolver-precedence | manual-balance precedence applied in all 8 latest-balance reads (goals, guardrails, grounding x3, surface queue, obligations) so a manual correction no longer silently reverts to the feed |
| 1367ba5 | apply-obligation-rough-edges (residual) | documented the `apply_obligation_instances` schema in the docstring |
| 0d4fbd2 | surfacing-lifecycle-broken (T3) | `due_date` folded into the surface change-detection hash so a date-only change updates the task in place instead of leaving a stale due date |

## Verified ALREADY fixed in code (do not redo)

- **same-date instance collision / silent no-op** (gap #8): `_next_instance_id` auto-suffixes `:1`/`:2`; `apply_obligation_instances` returns created/updated/instance_ids; `verification.py` flags duplicates as errors. Tests lock it in.
- **oversized-tool-outputs** (gap #13): `get_finance_status` and `list_obligations` already take `compact=`. Residual is only whether to flip the default to compact (a judgment call, not done — would change consumers).
- **todoist-write-tools, projection coverage signal, task-pileup lifecycle, permission profile, onboarding-count, hardcoded-config**: shipped on main before this branch (git log).
- **T4 (critical Apple Card item not on board)**: `_manual_obligation_due_surface_items` DOES surface discretionary items ("Decide amount + pay"). The board absence is surfacing-cadence/timing, not a code gap. Not a bug.

## Deferred — architecture / product judgment (need a decision, not blind code)

- **canonical-data-source-ambiguity** + **finance-memory-unreliable**: data-model decisions (single source of truth, memory write/read reliability). Multi-step; needs design sign-off.
- **no-variable-spend-budget**: needs a budgeting model (a new obligation `kind` or a separate budget table) — product decision.
- **drift-false-matches** (HIGH): the drift matcher links obligations to unrelated transactions. Real, but the matching heuristic needs careful redesign + a corpus to validate against; risk of regressing reconciliation if rushed.
- **digest-status-stale-conflated** (HIGH): one YELLOW conflates cash danger with a benign review queue, and stale review findings keep it yellow. Needs a status-severity model decision (split health axes).

## Deferred — cross-repo live launchd wrappers (NOT in this repo; `~/dev/admin` is dirty)

These are live scheduled-job scripts on the machine; `~/dev/admin` has uncommitted
WIP on `finance-surface-daily.sh`. Patches below are ready to apply by hand —
not auto-committed, to avoid clobbering that WIP and changing live automation
unreviewed.

### routines-die-on-usage-limit (HIGH)
`finance-surface-daily.sh` gates success on the `claude -p` exit code only, but a
weekly-usage-limit refusal exits 0 -> the wrapper writes a healthy heartbeat for a
day where nothing ran. Fix: capture stdout, and if it matches the usage-limit
refusal (or lacks the expected status line), force `status=failed`, skip the
healthy heartbeat, and for capture do NOT advance `.finance/last-capture.json` so
the window re-scans. Same gap in the (missing) `finance-capture-daily` wrapper.

### capture-routine-cannot-scan (HIGH)
The daily fact-capture routine is told to grep the transcript, but headless Bash is
denied and the Read tool refuses files over 256KB/25k tokens while transcripts run
4-9MB, so it silently degrades to a start/middle/end sample. Fix: have the wrapper
pre-slice the target transcript to the capture window and strip tool-schema/telemetry
lines before passing the path (or allowlist a read-only grep/jq for that routine),
and make a complete scan a hard requirement that fails loudly instead of sampling.

## Remaining open themes not yet addressed (backlog)

surface-queue-mislabeled-as-board (labeling/narration), obligation-lifecycle-hygiene,
reconcile-tools-overlap, surface-status-reporting-inconsistent, followup-capture-gaps
(no `update_followup`), no-bulk-onboarding-decisions, adversarial-enablement-scattered
(3 env edits), grounding-false-confidence (verify_grounding proves traceability, not
correctness), balance-freshness-invisible (partial — add a freshness signal to status),
mcp-version-staleness (partial — get_version/get_job_health exist), no-first-class-scheduler
(cross-repo). Each has git-checked evidence in the run output.
